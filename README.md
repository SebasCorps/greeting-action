# greetingMSPQ-action-test-v4
simple custom action for learning

**greetingMSPQ-action-test-v4** is a lightweight custom GitHub Action designed to demonstrate how composite actions work.
This action accepts a user's name and environment as input?, prints a greeting message, and exposes a reusable output that can be consumed in subsequent workflow steps.
